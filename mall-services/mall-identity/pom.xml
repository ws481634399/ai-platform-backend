<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.ai-mall</groupId>
    <artifactId>mall-services</artifactId>
    <version>1.0.0-SNAPSHOT</version>
  </parent>

  <artifactId>mall-identity</artifactId>

  <name>mall-identity</name>
  <description>身份认证服务：注册、登录、Token（业务代码留待后续里程碑）</description>

  <dependencies>
    <!-- WebMVC + 统一响应/异常/TraceId/OpenAPI（经 mall-common-web 传递，版本经 mall-bom） -->
    <dependency>
      <groupId>com.ai-mall</groupId>
      <artifactId>mall-common-web</artifactId>
      <version>${project.version}</version>
    </dependency>
    <!-- 日志基础配置资产（logback/mall-logback-base.xml 随 jar 提供） -->
    <dependency>
      <groupId>com.ai-mall</groupId>
      <artifactId>mall-common-log</artifactId>
      <version>${project.version}</version>
    </dependency>
    <!-- 数据访问基础：服务直连官方 starter，不经 common 中转（design.md §2.7） -->
    <dependency>
      <groupId>com.baomidou</groupId>
      <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
    </dependency>
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-core</artifactId>
    </dependency>
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-mysql</artifactId>
    </dependency>
    <dependency>
      <groupId>com.mysql</groupId>
      <artifactId>mysql-connector-j</artifactId>
      <scope>runtime</scope>
    </dependency>
    <!-- Nacos 服务发现预留：默认关闭（NACOS_ENABLED:false），禁启动断言（design.md §2.6） -->
    <dependency>
      <groupId>com.alibaba.cloud</groupId>
      <artifactId>spring-cloud-starter-alibaba-nacos-discovery</artifactId>
    </dependency>
    <!-- 测试依赖聚合器：test scope 引用，传递 JUnit5/AssertJ/Mockito/H2（design.md §2.8） -->
    <dependency>
      <groupId>com.ai-mall</groupId>
      <artifactId>mall-common-test</artifactId>
      <version>${project.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <!-- 可执行 Fat Jar：独立打包与启动 -->
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
    </plugins>
  </build>
</project>
